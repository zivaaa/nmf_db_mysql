import {defaultAllFieldsList, defaultNotProtectedList, defaultPrimaryList, defaultProtectedList} from "./consts.js";

export default [
    defaultAllFieldsList,
    defaultPrimaryList,
    defaultProtectedList,
    defaultNotProtectedList,
]